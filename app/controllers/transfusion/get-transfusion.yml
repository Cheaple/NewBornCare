parameters:
  - name: patientId
    in: path
    type: integer
    required: true

definitions:
  Transfusion:
    type: object
    properties:
      id:
        type: integer
      patientId:
        type: integer
      nurseId:
        type: integer
      name:
        type: string
      startTime:
        type: integer
      finishTime:
        type: integer
      status:
        type: integer
      drugCnt:
        type: integer
      vein:
        type: integer
      tool:
        type: integer
      info:
        type: string
      drug:
        type: array
        items:
          type: object
          properties:
            id:
              type: integer
            seq:
              type: integer
            drug:
              type: integer
            dose:
              type: integer
            rate:
              type: integer
            startTime:
              type: integer
            status:
              type: integer
responses:
  200:
    schema:
      $ref: '#/definitions/Transfusion'