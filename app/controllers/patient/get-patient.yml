parameters:
  - name: id
    in: query
    type: integer
    required: true

definitions:
  Patient:
    type: object
    properties:
      id:
        type: integer
      name:
        type: string
      gender:
        type: integer
      birthdate:
        type: integer
      palmprint:
        type: binary
      guardian:
        type: string
      guardianId:
        type: string
      relation:
        type: integer
      tel:
        type: integer
      status:
        type: integer
      inDate:
        type: string
      outDate:
        type: string
      department:
        type: integer
      room:
        type: integer
      bed:
        type: integer
      allergy:
        type: string

responses:
  200:
    schema:
      $ref: '#/definitions/Patient'