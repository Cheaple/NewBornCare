parameters:
  - name: department
    in: query
    type: integer
    required: false

definitions:
  PatientList:
    type: object
    properties:
      count:
        type: integer
      patient:
        type: array
        items:
          type: object
          properties:
            id:
              type: integer
            name:
              type: string
            gender:
              type: integer
          
responses:
  200:
    schema:
      $ref: '#/definitions/PatientList'