parameters:
  - name: adminId
    in: path
    type: integer
    example: 10
    required: true
definitions:
  AdminDelete:
    type: object
    properties:
      message:
        type: string
        example: "ok"
      id:
        type: integer
        example: 10
responses:
  200:
    schema:
      $ref: '#/definitions/AdminDelete'