definitions:
  DrugList:
    type: array
    items:
      type: object
      properties:
        id:
          type: integer
          example: 1
        name:
          type: string
          example: 抗生素
responses:
  200:
    schema:
      $ref: '#/definitions/DrugList'