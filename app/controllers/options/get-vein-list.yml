definitions:
  VeinList:
    type: array
    items:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string        
responses:
  200:
    schema:
      $ref: '#/definitions/VeinList'