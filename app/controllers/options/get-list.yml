definitions:
  List:
    type: object
    properties:
      department:
        $ref: '#/definitions/DepartmentList'
      vein:
        $ref: '#/definitions/VeinList'
      tool:
        $ref: '#/definitions/ToolList'
      drug:
        $ref: '#/definitions/DrugList'
          
responses:
  200:
    schema:
      $ref: '#/definitions/List'