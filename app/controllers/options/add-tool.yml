parameters:
  - name: name
    in: body
    type: string
    example: 静脉留置针
    required: true
  
definitions:
  ToolAdd:
    type: object
    properties:
      message:
        type: string
        example: ok
      id:
        type: integer
        example: 5
responses:
  200:
    schema:
      $ref: '#/definitions/ToolAdd'