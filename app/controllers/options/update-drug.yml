parameters:
  - name: drugId
    in: path
    type: integer
    required: true
  - name: name
    in: body
    type: string
    required: true

definitions:
  DrugUpdate:
    type: object
    properties:
      message:
        type: string
      id:
        type: integer
responses:
  200:
    schema:
      $ref: '#/definitions/DrugUpdate'